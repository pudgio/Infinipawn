package infinipawn;

import necesse.engine.modLoader.annotations.ModEntry;

@ModEntry
public class InfiniPawnMod {
    public static void init() {
        // Initialization logic for your mod (if needed)
        System.out.println("InfiniPawn mod loaded: PawnBroker shop wealth is now infinite!");
    }
}
